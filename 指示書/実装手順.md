🚀 Cursor への移行手順

1. プロジェクトフォルダの作成
   bashmkdir nouvation-corporate
   cd nouvation-corporate
2. Astro プロジェクトの初期化
   bashnpm create astro@latest . -- --template minimal --yes
   npm install
3. 必要なパッケージのインストール
   bashnpm install -D tailwindcss @astrojs/tailwind
   npx astro add tailwind

```

### 4. **ファイル構成**
```

nouvation-corporate/
├── src/
│ ├── layouts/
│ │ └── Layout.astro # 共通レイアウト
│ ├── components/
│ │ ├── Header.astro # ヘッダーコンポーネント
│ │ ├── Footer.astro # フッターコンポーネント
│ │ ├── FV.astro # First View
│ │ ├── News.astro # ニュースセクション
│ │ ├── SocialIssues.astro # 社会課題セクション
│ │ ├── Mission.astro # ミッションセクション
│ │ ├── Services.astro # サービスセクション
│ │ ├── Story.astro # ストーリーセクション
│ │ ├── Voice.astro # 利用者の声
│ │ └── FAQ.astro # Q&A
│ ├── pages/
│ │ └── index.astro # トップページ
│ └── styles/
│ └── global.css # グローバルスタイル
├── public/
│ ├── fonts/ # Web フォント（必要に応じて）
│ └── images/ # 画像ファイル
├── astro.config.mjs
├── tailwind.config.mjs
└── package.json 5. 各ファイルの作成手順
src/layouts/Layout.astro
astro---
interface Props {
title?: string;
}

## const { title = 'NOUVATION | 農業界に、情報で革新を。' } = Astro.props;

<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;700&family=Syne:wght@400;600;700;800&display=swap" rel="stylesheet">
  </head>
  <body class="font-sans bg-[#111111] text-[#E5E7EB] overflow-x-hidden">
    <div class="noise-texture"></div>
    <slot />
  </body>
</html>

<style is:global>
  body {
    font-family: 'Noto Sans JP', sans-serif;
  }
  
  .font-display {
    font-family: 'Syne', sans-serif;
  }
  
  .noise-texture::before {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 400 400' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulance type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)' opacity='0.03'/%3E%3C/svg%3E");
    pointer-events: none;
    z-index: 1;
    opacity: 0.5;
  }
  
  html {
    scroll-behavior: smooth;
  }
  
  .fade-up {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.8s ease-out, transform 0.8s ease-out;
  }
  
  .fade-up.visible {
    opacity: 1;
    transform: translateY(0);
  }
</style>

src/pages/index.astro
astro---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import FV from '../components/FV.astro';
import News from '../components/News.astro';
import SocialIssues from '../components/SocialIssues.astro';
import Mission from '../components/Mission.astro';
import Services from '../components/Services.astro';
import Story from '../components/Story.astro';
import Voice from '../components/Voice.astro';
import FAQ from '../components/FAQ.astro';
import Footer from '../components/Footer.astro';

---

<Layout>
  <Header />
  <main>
    <FV />
    <News />
    <SocialIssues />
    <Mission />
    <Services />
    <Story />
    <Voice />
    <FAQ />
  </main>
  <Footer />
</Layout>

<script>
  // Intersection Observer for fade-up animation
  const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px'
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
      }
    });
  }, observerOptions);

  document.querySelectorAll('.fade-up').forEach(el => observer.observe(el));
</script>

6. MicroCMS 連携の準備
   src/lib/microcms.ts を作成
   typescriptimport type { MicroCMSQueries } from 'microcms-js-sdk';
   import { createClient } from 'microcms-js-sdk';

const client = createClient({
serviceDomain: import.meta.env.MICROCMS_SERVICE_DOMAIN,
apiKey: import.meta.env.MICROCMS_API_KEY,
});

export type News = {
id: string;
createdAt: string;
updatedAt: string;
publishedAt: string;
revisedAt: string;
title: string;
category: string;
};

export const getNewsList = async (queries?: MicroCMSQueries) => {
return await client.get<{ contents: News[] }>({ endpoint: 'news', queries });
};

```

#### `.env` ファイルを作成
```

MICROCMS_SERVICE_DOMAIN=your-service-domain
MICROCMS_API_KEY=your-api-key 7. Tailwind 設定
tailwind.config.mjs
javascript/** @type {import('tailwindcss').Config} \*/
export default {
content: ['./src/**/\*.{astro,html,js,jsx,md,mdx,svelte,ts,tsx,vue}'],
theme: {
extend: {
fontFamily: {
sans: ['"Noto Sans JP"', 'sans-serif'],
display: ['"Syne"', 'sans-serif'],
},
colors: {
'deep-soil': '#111111',
'off-white': '#F4F4F5',
'neon-lime': '#D4FF00',
'agri-green': {
DEFAULT: '#1B5E20',
dark: '#1B5E20',
light: '#2e7d32',
},
'matte-black': '#1F2937',
'smoke-white': '#E5E7EB',
},
},
},
plugins: [],
} 8. 開発サーバーの起動
bashnpm run dev 9. 本番ビルド
bashnpm run build
npm run preview 10. Vercel へのデプロイ
bashnpm install -g vercel
vercel

📝 次のステップ

画像の差し替え: public/images/ に実際の画像を配置
MicroCMS 連携: API 設定後、News.astro でデータ取得
アニメーション調整: 必要に応じてタイミングや動きを調整
SEO 対策: メタタグ、OGP 画像の設定
パフォーマンス最適化: 画像の最適化、遅延読み込みの実装

これで実装準備は完了です！Cursor で開いて、コンポーネントごとに分割実装していってください。今作ってくれたコードは
index.astro？16:09 いえ、今作ったのは React コンポーネント です！
現在の Artifact は React (application/vnd.ant.react) で作成されているので、ブラウザ上でプレビューできる形になっています。
