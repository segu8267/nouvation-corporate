指示1: 初期セットアップの確認
以下を確認して、問題があれば修正してください：

1. package.jsonの依存関係が正しくインストールされているか確認
2. tailwind.config.mjsがastro.config.mjsで正しく読み込まれているか確認
3. src/layouts/Layout.astroのフォント読み込みが正しく動作するか確認

確認後、`npm run dev`を実行して、開発サーバーが正常に起動するかテストしてください。

指示2: レスポンシブデザインの最終確認
全コンポーネントのレスポンシブデザインを確認してください：

【確認ポイント】

- スマホ(375px)、タブレット(768px)、PC(1280px)での表示崩れ
- ハンバーガーメニューの開閉動作
- 画像のアスペクト比維持
- テキストの改行位置
- ボタンのタップ領域(最小44px)

問題があれば修正案を提示してください。

指示3: アニメーションの動作確認
以下のアニメーション・インタラクションが正しく動作するか確認してください：

1. スマートヘッダー（スクロールで表示/非表示）
2. Fade Upアニメーション（要素が画面に入った時の出現）
3. フッターのマーキーアニメーション
4. ホバーエフェクト（ボタン、カード）

動作しない箇所があれば、修正してください。

指示4: パフォーマンス最適化
以下のパフォーマンス最適化を実装してください：

1. 画像の遅延読み込み（loading="lazy"）を全画像に追加
2. 外部画像URLをWebP形式に変更（可能な範囲で）
3. フォントのpreloadを追加
4. CSSの重複削除とminify

最適化後、Lighthouseスコアを確認してください。

指示5: SEO対策の実装
以下のSEO対策を実装してください：

1. 各セクションに適切なセマンティックHTMLタグを使用
2. 画像にalt属性を追加
3. メタディスクリプション、OGPタグをLayout.astroに追加
4. sitemap.xmlとrobots.txtを生成

【追加してほしいメタタグ】

- og:image（OGP画像）
- og:description
- twitter:

指示6: アクセシビリティ対応
WCAG 2.1 AA基準に従って、以下のアクセシビリティ対応を実装してください：

1. 全ボタンとリンクにaria-labelを追加
2. キーボード操作（Tab、Enter）で全機能が使えるか確認
3. コントラスト比が4.5:1以上か確認（特に#D4FF00と背景色）
4. フォーカスインジケーターが視認できるか確認
5. スクリーンリーダー対応のランドマークを追加

問題があれば修正案を提示してください。

指示7: MicroCMS連携の準備
MicroCMS連携のための準備を行ってください：

1. `src/lib/microcms.ts`を作成
2. News.astroを修正して、microCMSからデータを取得できるように変更
3. .env.exampleファイルを作成（実際のキーは含めない）
4. エラーハンドリングを追加（APIが失敗した場合の表示）

【注意】

- 実際のAPIキーは.envに記載し、.gitignoreに含める
- フォールバックとしてダミーデータを残す

基本情報
API名
APIの内容を入力してください。
お知らせ
エンドポイント
APIのエンドポイント名を指定します。半角で入力してください。
https://nouvation.microcms.io/api/v1/

client
.get({
endpoint: 'news',
})
.then((res) => console.log(res));

指示8: エラーページの作成
404エラーページを作成してください：

ファイル: src/pages/404.astro

【デザイン要件】

- NOUVATIONのブランドカラーを使用
- 「ページが見つかりません」というメッセージ
- トップページへ戻るボタン
- ユーザーフレンドリーなデザイン

指示9: サイトマップの作成
HTMLサイトマップページを作成してください：

ファイル: src/pages/sitemap.astro

【内容】

- 全セクションへのリンク
- 法的ページ（プライバシーポリシー、特商法）
- シンプルで見やすいレイアウト

また、XML形式のサイトマップも生成してください。

指示10: ビルド確認と最終調整
本番ビルドを実行して、問題がないか確認してください：

1. `npm run build`を実行
2. ビルドエラーがないか確認
3. `npm run preview`で本番環境をローカル確認
4. 不要なコンソールログを削除
5. コメントアウトされた不要なコードを削除

問題があれば報告し、修正してください。

すでにリポジトリは作った
https://github.com/segu8267/nouvation-corporate.git

Step 3: リモートリポジトリに接続
Web UIで作成した場合：
bash# リモートリポジトリを追加
git remote add origin https://github.com/YOUR_USERNAME/nouvation-corporate.git

# メインブランチをpushに変更（必要に応じて）

git branch -M main

# 初回push

git push -u origin main

Step 4: ブランチ戦略の設定
bash# 開発用ブランチ作成
git checkout -b develop

# feature用ブランチ作成（例）

git checkout -b feature/microcms-integration
推奨ブランチ構成:

main: 本番環境（常に安定版）
develop: 開発環境（統合テスト用）
feature/_: 機能追加用
fix/_: バグ修正用

Step 5: Vercelとの連携（デプロイ設定）
方法A: Vercel CLI
bash# Vercel CLIをインストール
npm i -g vercel

# ログイン

vercel login

# デプロイ

vercel

# 本番デプロイ

vercel --prod

Step 6: 環境変数の管理
.env.example を作成
bash# .env.exampleファイルを作成
cat > .env.example << EOF

# MicroCMS

MICROCMS_SERVICE_DOMAIN=your-service-domain
MICROCMS_API_KEY=your-api-key

# その他の環境変数（必要に応じて）

SITE_URL=https://nouvation.com
EOF

そのまま使う手順
方法A: GitにPushするだけで自動デプロイ
bash# ローカルで変更をコミット
git add .
git commit -m "Update: コーポレートサイト実装完了"

# GitHubにpush

git push origin main

# .envファイルを作成（.gitignoreに含まれる）

cp .env.example .env

# エディタで実際の値を入力

code .env

```

---

## 📂 最終的なGit構成
```

nouvation-corporate/
├── .git/ # Gitディレクトリ（自動生成）
├── .gitignore # ✅ 既に作成済み
├── .env.example # 環境変数のテンプレート
├── .env # 実際の環境変数（Git管理外）
├── README.md # プロジェクト説明（後で作成）
├── package.json
├── astro.config.mjs
├── tailwind.config.mjs
├── src/
│ ├── layouts/
│ ├── components/
│ ├── pages/
│ └── lib/
└── public/
└── images/ # 画像ファイル用
