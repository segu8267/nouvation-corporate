---
// Voice (User Testimonials) Component

const voices = [
  {
    age: '30代・施設野菜',
    title: '「出荷の迷いがなくなりました」',
    text: '毎朝LINEでグラフを見てから出荷量を決めています。前年比が見れるのが本当に便利で、経営判断のスピードが上がりました。'
  },
  {
    age: '20代・新規就農',
    title: '「孤独感が消えました」',
    text: '同世代の農家さんが頑張っている様子を見るだけでモチベーションになります。'
  },
  {
    age: '50代・露地栽培',
    title: '「これなら私にも使えます」',
    text: '難しいアプリは苦手で避けてきましたが、いつも使っているLINEに届くので抵抗なく使えています。'
  }
];
---

<section data-section-theme="light" id="voice" class="py-24 md:py-32 bg-[#F0ECDE]">
  <div class="max-w-7xl mx-auto px-4 sm:px-6">
    <!-- Section Title -->
    <div class="text-center mb-20 fade-up">
      <h2 class="text-3xl md:text-5xl font-bold mb-4 leading-relaxed text-[#231815]"><span class="font-maru">VOICE</span></h2>
      <p class="text-[#231815]/70">利用者の声</p>
    </div>

    <!-- Voice Cards -->
    <div class="grid md:grid-cols-3 gap-8">
      {voices.map((voice) => (
        <div class="bg-white border border-[#231815]/10 rounded-2xl p-8 fade-up hover:border-[#2e7d32]/40 transition-all shadow-sm">
          <div class="flex items-center mb-6">
            {/* <div class="w-12 h-12 bg-[#2e7d32]/20 rounded-full mr-4 flex items-center justify-center text-[#231815] font-bold text-lg">
              {voice.age.charAt(0)}
            </div> */}
            <span class="font-bold text-sm text-[#231815]">{voice.age}</span>
          </div>
          
          <h3 class="font-bold text-xl mb-4 text-[#231815]">{voice.title}</h3>
          <p class="text-sm text-[#231815] leading-loose">{voice.text}</p>
        </div>
      ))}
    </div>
  </div>
</section>